version: 0.2

phases:
 install:
    runtime-versions:
      nodejs: 16.14.2
    pre_build:
      commands:
        - cd backend
        - npm install
    build:
      commands:
        - npm run build
        - npm run test
        