const app = `app`

const AppReducerTypes = {
    LOGIN: `${app}/LOGIN`,
    LOGOUT: `${app}/LOGOUT`,
    UPDATE_USER: `${app}/UPDATE_USER`,
}

const AppActionTypes = {

}

module.exports = {
    AppReducerTypes,
    AppActionTypes
};